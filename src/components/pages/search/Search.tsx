import React from 'react';
import SearchBar from '../../common/SearchBar';

const Search = () => {
    return (
        <div id="search">
            <SearchBar></SearchBar>
        </div>
    );
};

export default Search;